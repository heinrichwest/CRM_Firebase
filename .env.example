# API base URL (no trailing slash). Used for all REST API calls.
VITE_API_BASE_URL=https://crm-service.speccon.co.za

# Optional: set to "true" to use mock auth instead of the real API
# VITE_USE_MOCK_AUTH=false
