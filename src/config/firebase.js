/**
 * Firebase is not used for the backend.
 * This stub exists so any legacy imports don't pull in the Firebase SDK.
 * All data goes through the REST API (https://crm-service.speccon.co.za).
 */

export const app = null
export const secondaryApp = null
export const db = null
export const auth = null
export const secondaryAuth = null
